CMAKE_MINIMUM_REQUIRED(VERSION 3.0)
PROJECT(foo)
FIND_PACKAGE(ArrayFire REQUIRED)
INCLUDE_DIRECTORIES(${ArrayFire_INCLUDE_DIRS})
 
ADD_EXECUTABLE(EC_2D ecuacionCalor2D.cpp)
TARGET_LINK_LIBRARIES(EC_2D ${ArrayFire_CUDA_LIBRARIES} )
